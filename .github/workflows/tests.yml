name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies (tkinter)
        run: sudo apt-get update && sudo apt-get install -y python3-tk

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install numpy

      - name: Install API dependencies
        run: pip install -r requirements-api.txt

      - name: Secret scan (no hardcoded credentials)
        run: python scripts/check_secrets.py

      - name: Run test suite
        env:
          SECRET_KEY: ci-test-secret-key-minimum-32-chars-long
        run: python -m pytest test_shiftiq.py test_api.py -v

  api-smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies (tkinter)
        run: sudo apt-get update && sudo apt-get install -y python3-tk

      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-api.txt

      - name: Import api.py (smoke test)
        env:
          SECRET_KEY: ci-test-secret-key-minimum-32-chars-long
        run: python -c "import api; assert api.app; print('api.py imports cleanly')"

      - name: Run API tests
        env:
          SECRET_KEY: ci-test-secret-key-minimum-32-chars-long
        run: python -m pytest test_api.py -v
